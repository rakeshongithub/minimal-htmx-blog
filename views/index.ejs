<!DOCTYPE html>
<html>
  <head>
    <title>HTMX Blog</title>
  </head>
  <body>
    <h1>Blog Posts</h1>
    <ul>
      <% posts.forEach(post => { %>
      <li><a href="/post/<%= post.id %>"><%= post.title %></a></li>
      <% }) %>
    </ul>
  </body>
</html>
